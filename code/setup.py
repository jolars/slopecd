from setuptools import setup, find_packages

version = 0.1

DISTNAME = 'slope'
LICENSE = 'BSD (3-clause)'
VERSION = version

setup(name='slope',
      license=LICENSE,
      packages=find_packages()
      )
